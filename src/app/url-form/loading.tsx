import FormSkeletonShorten from "@/components/url/FormSkeletonShorten";

export default function Loading() {
  // You can add any UI inside Loading, including a Skeleton.
  return <FormSkeletonShorten />;
}
